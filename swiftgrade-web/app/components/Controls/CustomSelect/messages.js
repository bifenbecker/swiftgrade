import { defineMessages } from 'react-intl';

export default defineMessages({
  addRowsValue: {
    id: 'assessments.form.addRowsValue',
  },
  addRowValue: {
    id: 'assessments.form.addRowValue',
  },
});
