import { defineMessages } from 'react-intl';

export default defineMessages({
  countResults: {
    id: 'assessments.answerSheet.results.countResults',
  },
  oneResult: {
    id: 'assessments.answerSheet.results.oneResult',
  },
});
