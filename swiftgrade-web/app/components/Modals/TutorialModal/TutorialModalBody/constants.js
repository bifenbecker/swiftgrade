import { iconAfterRelease, iconDistributeRegularAS } from 'components/Lotties/';

export const AFTER_RELEASE_LOTTIE_OPTIONS = {
  loop: true,
  autoplay: true,
  animationData: iconAfterRelease,
  rendererSettings: {
    preserveAspectRatio: 'xMidYMid slice',
  },
};

export const DISTRIBUTE_REGULAR_AS_LOTTIE_OPTIONS = {
  loop: true,
  autoplay: true,
  animationData: iconDistributeRegularAS,
  rendererSettings: {
    preserveAspectRatio: 'xMidYMid slice',
  },
};
