import { defineMessages } from 'react-intl';

export default defineMessages({
  attachments: {
    id: 'assessments.settings.attachments',
  },
  assesmentFiles: {
    id: 'assessments.settings.attachments.tooltip',
  },
});
