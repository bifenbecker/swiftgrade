export const styles = theme => ({
  modal_text: {
    whiteSpace: 'pre-wrap',
    marginBottom: theme.spacing(1),
    fontSize: 16,
  },
  modal_buttons: {
    display: 'flex',
    justifyContent: 'flex-end',
    alignItems: 'flex-end',
    marginTop: 35,
  },
  modal_button: {
    marginRight: 20,
  },
});
